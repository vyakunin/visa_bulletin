[project]
name = "visa-bulletin-daily-checkup"
version = "0.1.0"
description = "Daily checkup MCP server for visa-bulletin.us — health, traffic, security signals."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27",
]

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8"]
