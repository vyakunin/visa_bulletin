version: '3.8'

# Green environment - runs on port 8001
# Alternate port (8001). With instance rotation, use one compose per instance (e.g. blue on prod, blue on staging).

services:
  redis:
    image: redis:7-alpine
    container_name: visa_bulletin_redis
    restart: unless-stopped
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru
    # Limit memory so Redis doesn't crash the instance; evict least-recently-used keys when full

  web-green:
    image: ghcr.io/vyakunin/visa_bulletin:${IMAGE_TAG:-latest}
    container_name: visa_bulletin_web_green
    ports:
      - "8001:8000"
    volumes:
      # PostgreSQL is used for database (no SQLite volume needed)
      - ./saved_pages:/app/saved_pages
      - ./logs:/app/logs
    environment:
      - DJANGO_SETTINGS_MODULE=django_config.settings
      - PYTHONUNBUFFERED=1
      - REDIS_URL=redis://redis:6379/1
      - WEB_CONCURRENCY=${WEB_CONCURRENCY:-2}
      - DB_HOST=host.docker.internal
      - ANALYTICS_SCRIPT=<script data-goatcounter="https://vyakunin.goatcounter.com/count" async src="https://gc.zgo.at/count.js"></script>
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-localhost,127.0.0.1,54.196.241.197,44.209.204.255,visa-bulletin.us,www.visa-bulletin.us}
    env_file:
      - ../.env
    depends_on:
      - redis
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    # Memory limit: 200MB for web container (prevents OOM on 1GB instance)
    # System needs ~200MB, Nginx ~20MB, data-refresh ~50MB, leaving ~200MB for web
    mem_limit: 200m
    memswap_limit: 200m
    # Override CMD: workers from WEB_CONCURRENCY (default 2). On 2GB staging use WEB_CONCURRENCY=1.
    command: sh -c "python3 manage.py migrate --noinput && gunicorn --workers $${WEB_CONCURRENCY:-2} --threads 2 --bind 0.0.0.0:8000 --timeout 60 --max-requests 500 --max-requests-jitter 50 --access-logformat '%(h)s %(l)s %(u)s %(t)s \"%(r)s\" %(s)s %(b)s %(L)s' django_config.wsgi:application"
    healthcheck:
      test: ["CMD", "curl", "-f", "--max-time", "5", "http://localhost:8000/"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    
